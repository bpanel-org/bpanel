const bcoin = require('bcoin');

// Check for existance of wallet.dat
// Read config.json, then call bcoin with different prameters accordingly

// wait for bcoin to start then fork and Run init script from config.json if there was no dat file.
